# 포트폴리오 GitHub Pages 데모 작업 체크리스트

## 🎯 프로젝트 목적 및 의도

### 목표
- **포트폴리오 설명 페이지**와 **라이브 데모**를 하나의 GitHub Pages에 통합
- 스크린샷/화면녹화 대신 **인터랙티브 데모**로 프로젝트 흐름 시연
- 백엔드 개발 역량(코드 품질, 아키텍처 설계)을 보여주는 것이 주 목적

### 핵심 전략
1. **"사용자 경험 구간"과 "설명 구간"을 명확히 분리**
   - 설명 섹션: 프로젝트 소개, 기술 스택, 아키텍처 설명
   - 시연 섹션: 더미데이터로 동작하는 프론트엔드 데모

2. **배포를 하지 않은 이유를 명확히 설명**
   - "포트폴리오 목적상 코드 품질과 아키텍처 설계에 집중했습니다"
   - "실제 운영 배포는 프로젝트 범위를 벗어나며, 필요 시 가능한 구조로 설계했습니다"
   - "GitHub 코드와 상세 문서로 구현 내용을 충분히 검증할 수 있습니다"

3. **데모 모드 명확히 표시**
   - "🎮 데모 모드 (더미 데이터)" 배너
   - "실제 백엔드 API는 GitHub 코드에서 확인 가능" 문구

### 작업 범위
- ✅ 포트폴리오 설명 페이지 (기존 유지 또는 개선)
- ✅ Petory 프론트엔드 컴포넌트 복사 및 통합
- ✅ API 모킹 레이어 추가 (더미데이터 반환)
- ✅ 설명/시연 섹션 분리 (탭 또는 라우팅)
- ❌ 실제 백엔드 서버 배포 (포트폴리오 범위 아님)

---

## 📋 작업 순서

### Phase 1: 프론트엔드 복사 및 기본 설정 (Vite 프로젝트 기준)

- [ ] **1.1 Petory 프론트엔드에서 복사할 파일/폴더 (명확한 목록)**
  
  **📍 복사 위치: `Petory/frontend/src/` → `포트폴리오프로젝트/src/`**
  
  **✅ 복사해야 할 것:**
  ```
  Petory/frontend/src/
  ├── components/          → 포트폴리오프로젝트/src/components/ (또는 src/demo/components/)
  ├── api/                 → 포트폴리오프로젝트/src/api/ (또는 src/demo/api/)
  ├── contexts/            → 포트폴리오프로젝트/src/contexts/ (또는 src/demo/contexts/)
  ├── hooks/               → 포트폴리오프로젝트/src/hooks/ (또는 src/demo/hooks/)
  └── styles/              → 포트폴리오프로젝트/src/styles/ (또는 src/demo/styles/)
  
  Petory/frontend/public/
  └── (이미지, 아이콘 등)  → 포트폴리오프로젝트/public/
  ```
  
  **❌ 복사하면 안 되는 것:**
  ```
  Petory/frontend/src/
  ├── App.js              ❌ (포트폴리오 App.js와 겹침, 참고만)
  ├── index.js            ❌ (Vite는 main.jsx 사용, 참고만)
  └── ...
  
  Petory/frontend/
  ├── package.json        ❌ (의존성만 참고, 직접 복사 X)
  ├── public/index.html   ❌ (Vite 형식이 다름, 참고만)
  └── ...
  ```
  
  **📋 복사 방법 (3가지 옵션):**
  
  **옵션 A: 데모 전용 폴더로 분리 (추천 ⭐)**
  ```bash
  # 포트폴리오 프로젝트에서 실행
  mkdir -p src/demo
  cp -r Petory/frontend/src/components src/demo/
  cp -r Petory/frontend/src/api src/demo/
  cp -r Petory/frontend/src/contexts src/demo/
  cp -r Petory/frontend/src/hooks src/demo/
  cp -r Petory/frontend/src/styles src/demo/
  ```
  **장점**: 포트폴리오 설명 페이지와 데모 완전 분리, 충돌 없음
  
  **옵션 B: src 루트에 직접 복사**
  ```bash
  # 포트폴리오 프로젝트에서 실행
  cp -r Petory/frontend/src/components src/
  cp -r Petory/frontend/src/api src/
  cp -r Petory/frontend/src/contexts src/
  cp -r Petory/frontend/src/hooks src/
  cp -r Petory/frontend/src/styles src/
  ```
  **주의**: 기존 파일과 겹치면 덮어쓰기됨 (백업 필수!)
  
  **옵션 C: components/Demo 하위로 복사**
  ```bash
  # 포트폴리오 프로젝트에서 실행
  mkdir -p src/components/Demo
  cp -r Petory/frontend/src/components/* src/components/Demo/
  cp -r Petory/frontend/src/api src/
  cp -r Petory/frontend/src/contexts src/
  cp -r Petory/frontend/src/hooks src/
  cp -r Petory/frontend/src/styles src/
  ```
  **장점**: 컴포넌트만 분리, 나머지는 공유

- [ ] **1.2 package.json 의존성 추가**
  ```json
  {
    "dependencies": {
      "react": "^19.2.0",
      "react-dom": "^19.2.0",
      "react-router-dom": "^6.x",
      "axios": "^1.13.0",
      "styled-components": "^6.1.19",
      "@react-google-maps/api": "^2.20.7",
      "recharts": "^3.4.1",
      "react-window": "^2.2.3"
    },
    "devDependencies": {
      "@vitejs/plugin-react": "^4.x",
      "vite": "^5.x",
      "gh-pages": "^6.x"
    },
    "scripts": {
      "dev": "vite",
      "build": "vite build",
      "preview": "vite preview",
      "predeploy": "npm run build",
      "deploy": "gh-pages -d dist"
    }
  }
  ```

- [ ] **1.3 vite.config.js 설정 (GitHub Pages용)**
  ```js
  import { defineConfig } from 'vite';
  import react from '@vitejs/plugin-react';

  export default defineConfig({
    plugins: [react()],
    base: '/[repo-name]/', // GitHub Pages 경로에 맞게 수정
    build: {
      outDir: 'dist',
    },
  });
  ```

- [ ] **1.4 필요한 패키지 설치**
  ```bash
  npm install
  npm install --save-dev gh-pages
  ```

- [ ] **1.5 index.html 수정 (Vite 형식)**
  - `public/index.html` 또는 루트의 `index.html` 확인
  - `<script type="module" src="/src/main.jsx"></script>` 형식으로 되어있는지 확인
  - CRA의 `%PUBLIC_URL%` → Vite의 `/` 경로로 변경

- [ ] **1.6 겹치는 파일 확인 및 처리**
  - 포트폴리오 프로젝트의 기존 파일 목록 확인
  - 겹치는 파일 목록 작성:
    - `src/App.js` / `src/App.jsx` → 기존 파일 백업 후 병합 필요
    - `src/main.jsx` → Vite 진입점이므로 유지, Petory의 `index.js`는 참고만
    - `package.json` → 의존성만 추가, 기존 스크립트 유지
    - `vite.config.js` → 기존 설정에 `base` 경로만 추가

---

### Phase 2: API 모킹 레이어 추가

- [ ] **2.1 API 모킹 유틸리티 생성**
  - 파일: `src/utils/apiMock.js` 생성
  - axios interceptor로 더미데이터 반환하도록 설정
  - 환경 변수로 모킹 모드 on/off 제어

- [ ] **2.2 더미데이터 폴더 구조 생성**
  ```
  src/
  └── mockData/
      ├── auth.js          # 로그인, 회원가입 더미데이터
      ├── boards.js        # 게시글 더미데이터
      ├── careRequests.js  # 펫케어 요청 더미데이터
      ├── users.js         # 유저 더미데이터
      ├── location.js      # 위치 서비스 더미데이터
      ├── chat.js          # 채팅 더미데이터
      └── admin.js         # 관리자 더미데이터
  ```

- [ ] **2.3 axios interceptor 설정**
  - `src/api/mockInterceptor.js` 생성
  - 모든 API 요청을 가로채서 더미데이터 반환
  - 실제 서버 호출 차단

---

### Phase 3: 더미데이터 생성

- [ ] **3.1 인증 관련 더미데이터**
  - 로그인 응답 (accessToken, refreshToken, user 정보)
  - 회원가입 응답
  - 토큰 검증 응답

- [ ] **3.2 게시글 더미데이터**
  - 게시글 목록 (페이징 포함)
  - 게시글 상세
  - 댓글 목록
  - 좋아요/싫어요 반응

- [ ] **3.3 펫케어 요청 더미데이터**
  - 케어 요청 목록
  - 케어 요청 상세
  - 지원 목록
  - 리뷰 목록

- [ ] **3.4 위치 서비스 더미데이터**
  - 주변 서비스 목록
  - 서비스 상세
  - 지도 마커 데이터

- [ ] **3.5 채팅 더미데이터**
  - 채팅방 목록
  - 메시지 목록
  - 실시간 메시지 (시뮬레이션)

- [ ] **3.6 관리자 더미데이터**
  - 유저 목록
  - 통계 데이터
  - 신고 목록

---

### Phase 4: 컴포넌트 수정

- [ ] **4.1 App.js 수정**
  - API 모킹 인터셉터 초기화 추가
  - 데모 모드 배너 추가
  - 설명 섹션과 시연 섹션 분리 (탭 또는 라우팅)
  - **의도**: "사용자 경험 구간"과 "설명 구간" 명확히 분리

- [ ] **4.2 네비게이션 수정**
  - "프로젝트 설명" 탭 추가
  - "라이브 데모" 탭 추가
  - 데모 모드 표시 배너

- [ ] **4.3 WebSocket/STOMP 관련 제거 또는 모킹**
  - 채팅 실시간 통신 모킹
  - 알림 SSE 모킹

- [ ] **4.4 파일 업로드 모킹**
  - 이미지 업로드 시 더미 이미지 URL 반환

---

### Phase 5: 설명 섹션 추가

- [ ] **5.1 프로젝트 소개 페이지 생성**
  - 파일: `src/components/Portfolio/ProjectIntro.js`
  - 프로젝트 개요
  - 기술 스택
  - 핵심 기능 요약
  - **배포 전략 섹션 추가** (중요!)
    - "왜 배포를 안 했는지" 명확히 설명
    - "포트폴리오 목적상 코드 품질과 아키텍처 설계에 집중"
    - "필요 시 배포 가능한 구조로 설계됨" 언급

- [ ] **5.2 아키텍처 설명 페이지 생성**
  - 파일: `src/components/Portfolio/Architecture.js`
  - 시스템 아키텍처 다이어그램
  - 도메인별 설명
  - 주요 설계 결정

- [ ] **5.3 성능 최적화 페이지 생성**
  - 파일: `src/components/Portfolio/Performance.js`
  - N+1 문제 해결
  - 캐싱 전략
  - 쿼리 최적화

- [ ] **5.4 트러블슈팅 페이지 생성**
  - 파일: `src/components/Portfolio/Troubleshooting.js`
  - 동시성 문제 해결
  - 주요 이슈 및 해결 과정

---

### Phase 6: 라우팅 설정

- [ ] **6.1 React Router 설정**
  - `src/App.js`에 라우팅 추가
  - basename 설정 (GitHub Pages용)
  ```jsx
  <BrowserRouter basename="/[repo-name]">
  ```
  - 또는 `vite.config.js`의 `base`와 동일하게 설정

- [ ] **6.2 라우트 구성**
  ```
  / → 홈 (프로젝트 소개)
  /demo → 라이브 데모
  /architecture → 아키텍처 설명
  /performance → 성능 최적화
  /troubleshooting → 트러블슈팅
  ```

---

### Phase 7: 스타일링 및 UX

- [ ] **7.1 데모 모드 배너 추가**
  - 상단 고정 배너: "🎮 데모 모드 (더미 데이터)"
  - "실제 백엔드 API는 GitHub 코드에서 확인 가능" 문구
  - **의도**: 데모임을 명확히 표시하여 오해 방지

- [ ] **7.2 설명/시연 전환 UI**
  - 탭 또는 사이드바로 섹션 전환
  - 현재 섹션 하이라이트

- [ ] **7.3 반응형 디자인 확인**
  - 모바일/태블릿/데스크톱 대응

---

### Phase 8: 배포 설정

- [ ] **8.1 GitHub Pages 설정**
  - 저장소 Settings → Pages
  - Source: `gh-pages` 브랜치 선택
  - Folder: `/ (root)` 선택
  - **주의**: Vite는 `dist` 폴더를 배포하므로 `gh-pages -d dist` 사용

- [ ] **8.2 빌드 및 배포 (Vite)**
  ```bash
  npm run build    # Vite는 dist 폴더에 빌드됨 (CRA는 build)
  npm run deploy   # gh-pages -d dist
  ```

- [ ] **8.3 배포 확인**
  - `https://[username].github.io/[repo-name]` 접속 확인
  - 모든 라우트 동작 확인
  - 더미데이터 정상 로드 확인

---

### Phase 9: 문서 링크 추가

- [ ] **9.1 GitHub 저장소 링크**
  - 메인 README 링크
  - 주요 문서 링크

- [ ] **9.2 상세 문서 링크**
  - 아키텍처 문서
  - 도메인별 문서
  - 성능 최적화 문서

---

## 🔧 핵심 작업 파일 목록

### 생성해야 할 파일
```
src/
├── utils/
│   └── apiMock.js              # API 모킹 유틸리티
├── api/
│   └── mockInterceptor.js      # axios interceptor 설정
├── mockData/
│   ├── auth.js
│   ├── boards.js
│   ├── careRequests.js
│   ├── users.js
│   ├── location.js
│   ├── chat.js
│   └── admin.js
└── components/
    └── Portfolio/
        ├── ProjectIntro.js      # 프로젝트 소개
        ├── Architecture.js      # 아키텍처 설명
        ├── Performance.js       # 성능 최적화
        ├── Troubleshooting.js   # 트러블슈팅
        └── DemoBanner.js       # 데모 모드 배너
```

### 수정해야 할 파일
```
src/
├── App.js / App.jsx            # 라우팅, 모킹 초기화 (기존 파일 수정)
├── main.jsx                    # Vite 진입점 (기존 파일 유지, Petory의 index.js 참고)
├── components/Layout/Navigation.js  # 탭 추가 (기존 파일 수정 또는 새로 생성)
├── package.json                # 의존성 추가, deploy 스크립트 추가 (기존 파일 수정)
└── vite.config.js             # base 경로 설정 (기존 파일 수정)
```

### 겹치는 파일 처리 가이드

#### 1. App.js / App.jsx
- **기존 포트폴리오 App.js**: 유지
- **Petory App.js**: 참고용으로만 사용
- **작업**: 기존 App.js에 Petory의 라우팅 로직 추가

#### 2. package.json
- **기존 스크립트**: 유지 (`dev`, `build`, `preview`)
- **추가할 것**: Petory 의존성 + `deploy` 스크립트
- **작업**: `dependencies`에 Petory 패키지 추가

#### 3. vite.config.js
- **기존 설정**: 유지
- **추가할 것**: `base: '/[repo-name]/'` 옵션
- **작업**: `export default defineConfig`에 `base` 추가

#### 4. 컴포넌트 폴더
- **옵션 A (추천)**: `src/components/Demo/` 하위로 복사
- **옵션 B**: 기존 `components` 폴더에 덮어쓰기 (백업 필수)

---

## ⚠️ 주의사항

1. **더미데이터 형식**
   - 실제 백엔드 API 응답 형식과 동일하게 작성
   - 면접관이 코드 확인 시 일관성 유지
   - **의도**: GitHub 코드와 데모의 일관성 유지

2. **환경 변수**
   - 개발: 실제 서버 연결 가능하도록 설정
   - 배포: 더미데이터만 사용하도록 설정
   - **의도**: 로컬 개발 시 실제 서버 테스트 가능, 배포 시 데모 모드

3. **불필요한 의존성**
   - WebSocket, STOMP 등 서버 연결 라이브러리는 모킹 처리
   - 또는 제거 (데모에서 불필요한 경우)
   - **의도**: 서버 없이도 동작하도록

4. **성능**
   - 더미데이터 로딩 시뮬레이션 (setTimeout으로 지연 추가)
   - 실제 사용자 경험과 유사하게
   - **의도**: 실제 서비스처럼 보이도록

5. **배포 관련 설명 (매우 중요!)**
   - 포트폴리오 페이지에 "배포 전략" 섹션 필수 추가
   - "왜 배포를 안 했는지" 명확히 설명
   - "배포는 안 했어요", "시간이 없어서요" 같은 회피 표현 절대 금지
   - **의도**: 면접관이 이해할 수 있도록 기술적/전략적 관점에서 설명

---

## 📝 체크리스트 사용법

1. 각 항목을 완료할 때마다 `[ ]` → `[x]`로 체크
2. Phase별로 순차적으로 진행
3. 각 Phase 완료 후 테스트
4. 문제 발생 시 해당 Phase 재검토

---

## 🚀 빠른 시작 명령어 (Vite 기준)

```bash
# 1. 포트폴리오 Vite 프로젝트로 이동
cd 포트폴리오프로젝트

# 2. 컴포넌트 및 소스 파일 복사
# - src/components, src/api, src/contexts 등 복사

# 3. package.json에 의존성 추가
npm install axios styled-components react-router-dom @react-google-maps/api recharts react-window
npm install --save-dev gh-pages

# 4. vite.config.js 설정 (base 경로 설정)

# 5. API 모킹 레이어 추가

# 6. 더미데이터 생성

# 7. 컴포넌트 수정

# 8. 빌드 및 배포 (Vite는 dist 폴더 사용)
npm run build
npm run deploy
```

## ⚠️ Vite vs CRA 주요 차이점

### 빌드 출력 폴더
- **CRA**: `build/` 폴더
- **Vite**: `dist/` 폴더

### 설정 파일
- **CRA**: `package.json`의 `homepage` 필드
- **Vite**: `vite.config.js`의 `base` 옵션

### 개발 서버
- **CRA**: `npm start` (react-scripts)
- **Vite**: `npm run dev` (vite)

### 환경 변수
- **CRA**: `REACT_APP_*` 접두사
- **Vite**: `VITE_*` 접두사 (또는 `import.meta.env.*`)

### 정적 파일 경로
- **CRA**: `%PUBLIC_URL%` 사용
- **Vite**: `/` 또는 `import` 문 사용

---

---

## 💡 핵심 의도 요약

### 왜 이렇게 하는가?
1. **인터랙티브 데모 > 정적 스크린샷**
   - 스크린샷/화면녹화보다 실제 UI/UX를 직접 체험하는 게 효과적

2. **설명과 시연 통합**
   - 한 페이지에서 설명과 체험을 모두 제공
   - 하지만 "사용자 경험 구간"과 "설명 구간"은 명확히 분리

3. **배포는 하지 않지만 이유를 명확히**
   - "포트폴리오 목적상 코드 품질과 아키텍처 설계에 집중"
   - "실제 운영 배포는 프로젝트 범위를 벗어남"
   - "필요 시 배포 가능한 구조로 설계됨"

4. **데모 모드 명확히 표시**
   - 더미데이터 사용임을 명확히 표시
   - 실제 백엔드 API는 GitHub 코드에서 확인 가능

---

**작업 시작 전 이 체크리스트를 복사해서 사용하세요!**
