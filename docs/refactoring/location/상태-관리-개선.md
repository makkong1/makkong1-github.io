# 상태 관리 개선 리팩토링

**작성일**: 2026-02-04  
**리팩토링 목적**: 다수의 useState를 논리적으로 그룹화하여 코드 가독성 및 유지보수성 향상

---

## 📋 개요

### 현재 문제점

**상태 개수**:
- `useState`: 약 24개
- `useRef`: 약 6개
- 총 30개의 상태 관리 훅 사용

**문제점**:
- 상태가 많아 코드 가독성 저하
- 관련된 상태들이 분산되어 있어 관리 어려움
- 상태 업데이트 로직이 복잡함

### 해결 방안

관련된 상태들을 논리적으로 그룹화하여 `useReducer`로 통합.

---

## 🔍 현재 상태 분석

### 상태 분류

#### 1. 서비스 데이터 상태 (4개)
```javascript
const [allServices, setAllServices] = useState([]);
const [services, setServices] = useState([]);
const [availableSigungus, setAvailableSigungus] = useState([]);
const [availableEupmyeondongs, setAvailableEupmyeondongs] = useState([]);
```

#### 2. 검색 상태 (5개)
```javascript
const [keyword, setKeyword] = useState('');
const [selectedKeywordCategory, setSelectedKeywordCategory] = useState('');
const [addressQuery, setAddressQuery] = useState('');
const [categoryType, setCategoryType] = useState(CATEGORY_DEFAULT);
const [searchMode, setSearchMode] = useState('keyword');
```

#### 3. 지역 선택 상태 (4개)
```javascript
const [selectedSido, setSelectedSido] = useState('');
const [selectedSigungu, setSelectedSigungu] = useState('');
const [selectedEupmyeondong, setSelectedEupmyeondong] = useState('');
const [currentView, setCurrentView] = useState('sido');
```

#### 4. UI 상태 (8개)
```javascript
const [loading, setLoading] = useState(false);
const [error, setError] = useState(null);
const [statusMessage, setStatusMessage] = useState('지도 준비 중...');
const [selectedService, setSelectedService] = useState(null);
const [hoveredService, setHoveredService] = useState(null);
const [showDirections, setShowDirections] = useState(false);
const [showKeywordControls, setShowKeywordControls] = useState(false);
const [showRegionControls, setShowRegionControls] = useState(false);
```

#### 5. 지도 관련 상태 (5개)
```javascript
const [userLocation, setUserLocation] = useState(null);
const [userLocationAddress, setUserLocationAddress] = useState(null);
const [mapCenter, setMapCenter] = useState(null);
const [mapLevel, setMapLevel] = useState(10);
const [currentMapView, setCurrentMapView] = useState('nation');
```

#### 6. 기타 상태 (3개)
```javascript
const [directionsData, setDirectionsData] = useState(null);
const [startLocationAddress, setStartLocationAddress] = useState(null);
const [hoveredSido, setHoveredSido] = useState(null);
const [pendingSearchLocation, setPendingSearchLocation] = useState(null);
const [showSearchButton, setShowSearchButton] = useState(false);
```

#### 7. Ref 상태 (6개)
```javascript
const isProgrammaticMoveRef = useRef(false);
const isSearchModeRef = useRef(false);
const latestRequestRef = useRef(0);
const fetchServicesRef = useRef(null);
const isInitialLoadRef = useRef(true);
const initialLoadTypeRef = useRef(null);
const mapIdleTimeoutRef = useRef(null);
```

---

## 🔧 개선 방안

### 방안 1: 객체로 그룹화 (간단, 권장) ⭐

**가장 쉬운 방법**: 관련된 상태들을 하나의 객체로 묶기

**장점**:
- ✅ useState만 사용 (추가 학습 불필요)
- ✅ 빠른 구현 (기존 코드 수정 최소화)
- ✅ 코드 가독성 향상

**단점**:
- 상태 업데이트 시 전체 객체 복사 필요 (하지만 영향 미미)

**예시**:
```javascript
// Before: 5개의 개별 useState
const [keyword, setKeyword] = useState('');
const [selectedKeywordCategory, setSelectedKeywordCategory] = useState('');
const [addressQuery, setAddressQuery] = useState('');
const [categoryType, setCategoryType] = useState(CATEGORY_DEFAULT);
const [searchMode, setSearchMode] = useState('keyword');

// After: 1개의 객체로 묶기
const [searchState, setSearchState] = useState({
  keyword: '',
  selectedKeywordCategory: '',
  addressQuery: '',
  categoryType: CATEGORY_DEFAULT,
  searchMode: 'keyword',
});

// 사용법
setSearchState({ ...searchState, keyword: '동물병원' });
setSearchState({ ...searchState, categoryType: '동물병원' });
```

---

### 방안 2: useReducer로 그룹화 (고급)

**useReducer란?**: 여러 상태를 하나로 묶고, 업데이트 로직을 함수로 관리하는 방법

**장점**:
- 상태 업데이트 로직 중앙화
- 복잡한 상태 업데이트에 유리

**단점**:
- 학습 필요 (useReducer 개념 이해 필요)
- 초기 구현 시간 소요

**useReducer 간단 설명**:
```javascript
// useReducer는 useState의 고급 버전
// 상태와 업데이트 함수 대신, reducer 함수와 action을 사용

// 1. 초기 상태 정의
const initialState = {
  keyword: '',
  categoryType: CATEGORY_DEFAULT,
};

// 2. reducer 함수 정의 (상태를 어떻게 업데이트할지 정의)
const searchReducer = (state, action) => {
  switch (action.type) {
    case 'SET_KEYWORD':
      return { ...state, keyword: action.payload };
    case 'SET_CATEGORY':
      return { ...state, categoryType: action.payload };
    default:
      return state;
  }
};

// 3. useReducer 사용
const [searchState, dispatch] = useReducer(searchReducer, initialState);

// 4. 상태 업데이트
dispatch({ type: 'SET_KEYWORD', payload: '동물병원' });
dispatch({ type: 'SET_CATEGORY', payload: '동물병원' });
```

**언제 사용하나?**:
- 상태 업데이트 로직이 복잡할 때
- 여러 상태를 함께 업데이트해야 할 때
- 상태 업데이트 로직을 재사용하고 싶을 때

---

## ✅ 적용 방안: 객체로 그룹화 (간단한 방법)

### 1. 검색 상태 그룹화 (가장 간단한 방법)

**Before**: 5개의 개별 useState
```javascript
const [keyword, setKeyword] = useState('');
const [selectedKeywordCategory, setSelectedKeywordCategory] = useState('');
const [addressQuery, setAddressQuery] = useState('');
const [categoryType, setCategoryType] = useState(CATEGORY_DEFAULT);
const [searchMode, setSearchMode] = useState('keyword');
```

**After**: 1개의 객체로 묶기
```javascript
const [searchState, setSearchState] = useState({
  keyword: '',
  selectedKeywordCategory: '',
  addressQuery: '',
  categoryType: CATEGORY_DEFAULT,
  searchMode: 'keyword',
});
```

**사용 예시**:
```javascript
// Before
setKeyword('동물병원');
setCategoryType('동물병원');

// After (객체 스프레드 사용)
setSearchState({ ...searchState, keyword: '동물병원' });
setSearchState({ ...searchState, categoryType: '동물병원' });

// 또는 헬퍼 함수 사용 (더 편리)
const updateSearchState = (updates) => {
  setSearchState({ ...searchState, ...updates });
};

updateSearchState({ keyword: '동물병원', categoryType: '동물병원' });
```

**접근 방법**:
```javascript
// Before
const currentKeyword = keyword;
const currentCategory = categoryType;

// After
const currentKeyword = searchState.keyword;
const currentCategory = searchState.categoryType;
```

---

### 2. 지역 선택 상태 그룹화

**Before**: 4개의 개별 useState
```javascript
const [selectedSido, setSelectedSido] = useState('');
const [selectedSigungu, setSelectedSigungu] = useState('');
const [selectedEupmyeondong, setSelectedEupmyeondong] = useState('');
const [currentView, setCurrentView] = useState('sido');
```

**After**: 1개의 객체로 묶기
```javascript
const [regionState, setRegionState] = useState({
  selectedSido: '',
  selectedSigungu: '',
  selectedEupmyeondong: '',
  currentView: 'sido',
});
```

**사용 예시**:
```javascript
// Before
setSelectedSido('서울특별시');
setSelectedSigungu(''); // 초기화
setSelectedEupmyeondong(''); // 초기화
setCurrentView('sigungu');

// After (한 번에 업데이트 가능)
setRegionState({
  ...regionState,
  selectedSido: '서울특별시',
  selectedSigungu: '',
  selectedEupmyeondong: '',
  currentView: 'sigungu',
});

// 또는 헬퍼 함수 사용
const updateRegionState = (updates) => {
  setRegionState({ ...regionState, ...updates });
};

updateRegionState({
  selectedSido: '서울특별시',
  selectedSigungu: '',
  selectedEupmyeondong: '',
  currentView: 'sigungu',
});
```

---

### 3. UI 상태 그룹화

**Before**: 8개의 개별 useState
```javascript
const [loading, setLoading] = useState(false);
const [error, setError] = useState(null);
const [statusMessage, setStatusMessage] = useState('지도 준비 중...');
const [selectedService, setSelectedService] = useState(null);
const [hoveredService, setHoveredService] = useState(null);
const [showDirections, setShowDirections] = useState(false);
const [showKeywordControls, setShowKeywordControls] = useState(false);
const [showRegionControls, setShowRegionControls] = useState(false);
```

**After**: 1개의 객체로 묶기
```javascript
const [uiState, setUIState] = useState({
  loading: false,
  error: null,
  statusMessage: '지도 준비 중...',
  selectedService: null,
  hoveredService: null,
  showDirections: false,
  showKeywordControls: false,
  showRegionControls: false,
});
```

**사용 예시**:
```javascript
// Before
setLoading(true);
setError(null);
setStatusMessage('데이터 불러오는 중...');

// After (한 번에 업데이트 가능)
setUIState({
  ...uiState,
  loading: true,
  error: null,
  statusMessage: '데이터 불러오는 중...',
});

// 또는 헬퍼 함수 사용
const updateUIState = (updates) => {
  setUIState({ ...uiState, ...updates });
};

updateUIState({
  loading: true,
  error: null,
  statusMessage: '데이터 불러오는 중...',
});
```

---

## 📊 개선 효과

### Before: 24개의 개별 useState

```javascript
const [allServices, setAllServices] = useState([]);
const [services, setServices] = useState([]);
const [loading, setLoading] = useState(false);
const [error, setError] = useState(null);
const [keyword, setKeyword] = useState('');
const [selectedSido, setSelectedSido] = useState('');
// ... 18개 더
```

### After: 5개의 그룹화된 useState

```javascript
// 검색 상태 (5개 → 1개)
const [searchState, setSearchState] = useState({
  keyword: '',
  selectedKeywordCategory: '',
  addressQuery: '',
  categoryType: CATEGORY_DEFAULT,
  searchMode: 'keyword',
});

// 지역 선택 상태 (4개 → 1개)
const [regionState, setRegionState] = useState({
  selectedSido: '',
  selectedSigungu: '',
  selectedEupmyeondong: '',
  currentView: 'sido',
});

// UI 상태 (8개 → 1개)
const [uiState, setUIState] = useState({
  loading: false,
  error: null,
  statusMessage: '지도 준비 중...',
  selectedService: null,
  hoveredService: null,
  showDirections: false,
  showKeywordControls: false,
  showRegionControls: false,
});

// 서비스 데이터 상태 (4개 → 1개)
const [dataState, setDataState] = useState({
  allServices: [],
  services: [],
  availableSigungus: [],
  availableEupmyeondongs: [],
});

// 지도 상태 (5개 → 1개)
const [mapState, setMapState] = useState({
  userLocation: null,
  userLocationAddress: null,
  mapCenter: null,
  mapLevel: 10,
  currentMapView: 'nation',
});
```

**개선 효과**:
- ✅ 코드 가독성 향상: 관련 상태가 그룹화됨
- ✅ 유지보수성 향상: 상태 업데이트 로직 중앙화
- ✅ 버그 감소: 상태 업데이트 로직이 명확해짐
- ✅ 테스트 용이성 향상: reducer 함수를 독립적으로 테스트 가능

---

## 🧪 적용 계획

### 단계별 적용 (간단한 방법)

1. **1단계**: 검색 상태 그룹화 (5개 → 1개 객체)
   - `keyword`, `selectedKeywordCategory`, `addressQuery`, `categoryType`, `searchMode` → `searchState`
   - 예상 시간: 30분~1시간

2. **2단계**: 지역 선택 상태 그룹화 (4개 → 1개 객체)
   - `selectedSido`, `selectedSigungu`, `selectedEupmyeondong`, `currentView` → `regionState`
   - 예상 시간: 30분~1시간

3. **3단계**: UI 상태 그룹화 (8개 → 1개 객체)
   - `loading`, `error`, `statusMessage`, `selectedService`, `hoveredService`, `showDirections`, `showKeywordControls`, `showRegionControls` → `uiState`
   - 예상 시간: 1시간

4. **4단계**: 서비스 데이터 상태 그룹화 (4개 → 1개 객체)
   - `allServices`, `services`, `availableSigungus`, `availableEupmyeondongs` → `dataState`
   - 예상 시간: 30분~1시간

5. **5단계**: 지도 관련 상태 그룹화 (5개 → 1개 객체)
   - `userLocation`, `userLocationAddress`, `mapCenter`, `mapLevel`, `currentMapView` → `mapState`
   - 예상 시간: 30분

### 주의사항

- ✅ 점진적으로 적용하여 기존 기능 동작 확인
- ✅ 각 단계마다 테스트 수행
- ✅ 기존 코드와의 호환성 유지
- ✅ 헬퍼 함수 사용으로 코드 간소화

### 헬퍼 함수 예시

```javascript
// 각 상태 그룹별로 헬퍼 함수 생성
const updateSearchState = useCallback((updates) => {
  setSearchState(prev => ({ ...prev, ...updates }));
}, []);

const updateRegionState = useCallback((updates) => {
  setRegionState(prev => ({ ...prev, ...updates }));
}, []);

const updateUIState = useCallback((updates) => {
  setUIState(prev => ({ ...prev, ...updates }));
}, []);

// 사용 예시
updateSearchState({ keyword: '동물병원', categoryType: '동물병원' });
updateRegionState({ selectedSido: '서울특별시', currentView: 'sigungu' });
updateUIState({ loading: true, error: null });
```

---

## 📝 관련 문서

- [위치 기반 서비스 상세 분석](../../analysis/location/위치-기반-서비스-상세-분석.md)
- [프론트엔드 검색 로직 단순화](./프론트엔드-검색-로직-단순화.md)

---

**리팩토링 시작일**: 2026-02-04  
**리팩토링 완료일**: 2026-02-04

## ✅ 적용 완료 내용

### 완료된 그룹화

1. ✅ **검색 상태 그룹화** (5개 → 1개 reducer)
   - `keyword`, `selectedKeywordCategory`, `addressQuery`, `categoryType`, `searchMode`
   - `searchReducer` 및 `dispatchSearch` 적용 완료

2. ✅ **지역 선택 상태 그룹화** (4개 → 1개 reducer)
   - `selectedSido`, `selectedSigungu`, `selectedEupmyeondong`, `currentView`
   - `regionReducer` 및 `dispatchRegion` 적용 완료
   - 시도 선택 시 시군구/읍면동 자동 초기화 로직 포함

3. ✅ **UI 상태 그룹화** (8개 → 1개 reducer)
   - `loading`, `error`, `statusMessage`, `selectedService`, `hoveredService`, `showDirections`, `showKeywordControls`, `showRegionControls`
   - `uiReducer` 및 `dispatchUI` 적용 완료

### 개선 효과

- ✅ **코드 가독성 향상**: 24개의 개별 useState → 3개의 useReducer
- ✅ **상태 업데이트 로직 중앙화**: 관련된 상태를 함께 관리
- ✅ **버그 감소**: 상태 업데이트 로직이 명확해짐
- ✅ **유지보수성 향상**: 상태 그룹별로 관리하여 수정 용이
